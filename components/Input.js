import React from 'react';
import { View, TextInput, ScrollView, StyleSheet, KeyboardAvoidingView } from 'react-native';

const Input = props => {};

const styles = StyleSheet.create({});

export default Input;